fn main() {
    text := "Hello\nHow are you?"
    println(text)
    // println(lines(text))
    // println(split(text, ["\n"]))
    // println(starts_with(text, "hello"))
    // println(ends_with(text, "you?"))
    println(to_lowercase(text))
}
