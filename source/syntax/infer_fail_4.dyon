fn main() {
    x := [[1, 2], [3, 4], [5, 6]]
    for i {
        for j 1 {
            println("i j " + str(i) + " " + str(i))
            // `k` is before `i`, so it should fail.
            k := 0
            x[k][i] = clone(k)
        }
    }
}
