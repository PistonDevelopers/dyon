fn main() {
    x := [[1, 2], [3, 4], [5, 6]]
    for i {
        for j 1 {
            println("i j " + to_string(i) + " " + to_string(i))
            // `i` is not declared, so that's fine.
            k := 0
            x[i][k] = clone(k)
        }
    }
}
