fn main() {
    x := [[1, 2], [3, 4], [5, 6]]
    for i {
        for j 1 {
            println("i j " + str(i) + " " + str(i))
            // `i` is not declared, so that's fine.
            k := 0
            x[i][k] = clone(k)
        }
    }
}
