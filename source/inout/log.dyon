fn main() {
    for i 100 {
        inbox := in log \(msg: str) = true

        run()

        loop {
            res := try_next(inbox)
            if res == none() {break}
            res := unwrap(res)
            print(res[0])
        }
    }
}

fn log(msg: str) {}

fn run() {
    for i 10 {
        log(str(i))
    }
}
